/bad-pcb
/target
# project-local rustup state lives in .rustup; rustup itself is the
# dispatcher, the toolchain is a Nix-store symlink seeded by flake.nix.
.cargo-home
.rustup
.direnv
result
result-*
espflash.toml
partitions.csv
sdkconfig.defaults
sdkconfig.defaults.partitions
*.bin
out.txt
rustc-ice-*.txt
node_modules
/frontend/dist
/server/dist
/central-control/dist
